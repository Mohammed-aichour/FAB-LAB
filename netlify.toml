[build]
  command = "npm run build"
  publish = "docs"

[build.environment]
  NODE_VERSION = "20"

# SPA Fallback for React Router (Prevents 404 on page refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

